# 鞋类广告工作台设计规范

## 设计目标

- 面向批量电商广告生产，不做营销落地页。
- 第一屏直接完成上传、设置、运行和复制，不放大段功能介绍。
- 桌面端三栏同高，页面本身不滚动；内容过多时只在对应栏内滚动。
- 视觉层级依靠间距、边框、字号和少量底色，不依赖重阴影或大量卡片。

## 布局

- 顶栏：产品名、服务状态、API 设置图标。
- 左栏：上传预览、商品图片历史和选择操作。
- 中栏：国家、人群、并发数、模式、进度和批量队列。
- 右栏：两段 Flow Omni 生成包和脚本明细。
- 桌面端栏间距 `12px`，面板圆角 `8px`。

## 视觉令牌

- 页面背景：`#edf1f4`
- 主面板：`#ffffff`
- 主文字：`#172027`
- 次要文字：`#66727d`
- 边框：`#d6dde3`
- 主操作色：`#0d7f73`
- 危险操作色：`#a23d3d`
- 面板阴影：`0 8px 24px rgba(31, 43, 51, .06)`

## 组件规则

- 熟悉的工具操作优先使用图标按钮，并提供 `title` 和 `aria-label`。
- 主操作每个区域只保留一个高强调按钮。
- 选择项使用 `select`，二元设置使用开关。
- API 设置使用模态窗口，不占用日常工作区高度。
- 结果标签只显示用户判断任务所需的信息，不显示内部 Run ID 或 Prompt 数量。
- 两个 10 秒生成包使用同尺寸并排布局，复制按钮位置一致。

## 内容规则

- 上传区域在有图片后直接显示预览。
- 重复图片按文件内容自动跳过，不依赖文件名。
- Flow Omni 区只保留需要上传的图片类型和可复制文案。
- 图片 Prompt 保存在后台，不在主界面展示。
