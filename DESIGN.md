# 鞋类广告工作台设计规范

## 设计目标

- 面向个人鞋类广告制作，不做营销落地页。
- 用清晰的步骤引导上传、识别、创意、脚本和故事板生成。
- 关键确认和返回操作优先放在当前步骤顶部。
- 脚本和故事板尽量在一个屏幕内并排比较，减少无意义滚动。
- 视觉层级依靠间距、边框、字号、动效和少量底色。

## 布局

- 顶栏：产品名、项目入口、服务状态和 API 设置。
- 项目页：项目列表支持选择和批量删除。
- 素材页：上传预览和可用的单图删除操作。
- 第二步：产品锁定与创意信息直接填写，尺寸使用可视化选择器。
- 脚本页：左右并排显示两段 10 秒脚本，每个镜头只展示一组完整字段。
- 最终页：只展示两张故事板图片和两段对应脚本。

## 视觉令牌

- 页面背景：`#edf1f4`
- 主面板：`#ffffff`
- 主文字：`#172027`
- 次要文字：`#66727d`
- 边框：`#d6dde3`
- 主操作色：`#0d7f73`
- 危险操作色：`#a23d3d`
- 面板阴影：`0 8px 24px rgba(31, 43, 51, .06)`

## 组件规则

- 熟悉的工具操作优先使用图标按钮，并提供 `title` 和 `aria-label`。
- 主操作每个区域只保留一个高强调按钮。
- 选择项使用 `select`，二元设置使用开关。
- API 设置使用模态窗口，不占用日常工作区高度。
- 结果标签只显示用户判断任务所需的信息，不显示内部 Run ID 或 Prompt 数量。
- 两段 10 秒脚本使用同尺寸并排布局，复制按钮位置一致。

## 内容规则

- 上传区域在有图片后直接显示预览。
- 重复图片按文件内容自动跳过，不依赖文件名。
- 最终交付不显示 JSON 下载、旧版生成包或内部提示词。
- 图片提示词保存在后台，不在主界面展示。
