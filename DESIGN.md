# 鞋类广告工作台设计规范

## 设计目标

- 面向个人鞋类广告制作，不做营销落地页。
- 用清晰的五步流程引导商品素材、产品设定、生成脚本、生成故事版和生成视频。
- 关键确认和返回操作优先放在当前步骤顶部。
- 桌面端第一屏就是可用工作台，用户第一眼能看到当前项目、当前步骤和下一步按钮。
- 视觉层级依靠间距、边框、字号、动效和少量底色。

## 布局

- 左侧栏：产品名、新建项目、最近项目和本地保存状态。
- 顶部：项目名、单版/双版模式切换、当前步骤说明。
- 步骤条：五步固定显示，不使用技术状态名。
- 主工作区：只承载当前步骤的核心任务。
- 右侧栏：当前步骤检查器，例如 AI 素材检测器、AI 产品检查器、AI 脚本检查官。
- 生成视频：展示视频任务、故事版输入、脚本摘要、预览和下载入口。

## 视觉令牌

- 页面背景：`#f3f3ee`
- 左侧栏：`#171916`
- 主面板：`#fbfbf7`
- 主文字：`#171916`
- 次要文字：`#6f746d`
- 边框：`#dcded7`
- 主操作色：`#ddff3f`
- 主操作描边：`#a8ca00`
- 危险操作色：`#a23d3d`
- 面板阴影：`0 18px 60px rgba(28, 32, 24, 0.08)`

## 组件规则

- 熟悉的工具操作优先使用图标按钮，并提供 `title` 和 `aria-label`。
- 主操作每个区域只保留一个高强调按钮。
- 单版/双版使用分段控件，不显示 `single_video` 或 `legacy_multi_segment`。
- 普通前台不显示 API Key、供应商、模型和复杂调试项。
- 结果标签只显示用户判断任务所需的信息，不显示内部 Run ID、Prompt 数量或供应商请求细节。
- 脚本和故事版按当前模式展示：单版是一条完整脚本和一张故事版；双版是两个 10 秒段和两张故事版。

## 内容规则

- 上传区域在有图片后直接显示预览。
- 重复图片按文件内容自动跳过，不依赖文件名。
- 生成视频页不显示 JSON 下载、旧版生成包或内部提示词。
- 图片提示词保存在后台，不在主界面展示。
- 生成失败必须说明用户能理解的原因，并保留重试入口。
