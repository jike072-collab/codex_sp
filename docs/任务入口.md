# 任务入口

## 当前任务目标

把 `studio-v2` 做成小白用户能顺着完成的鞋类图片生成视频工作台。优先级按 P0/P1/P2 推进：

- P0：直接影响用户能否完成流程。
- P1：提升体验一致性和状态清晰度。
- P2：动画、细节 polish、文档补充和去重。

## 本机状态

- 本机代码位置：`E:\codex工作台\P001-codex_sp仓库`
- 当前仓库：`https://github.com/jike072-collab/codex_sp`
- 当前审查分支：`codex/night-workbench-review`
- 当前 commit：以 `git rev-parse HEAD` 为准；最新已记录 checkpoint 为 `93600f0 Add p0 flow smoke evidence`。
- GitHub 同步状态：`HEAD` 与 `origin/codex/night-workbench-review` 一致，`git rev-list --left-right --count HEAD...origin/codex/night-workbench-review` 为 `0 0`。
- 启动入口：`powershell -NoProfile -ExecutionPolicy Bypass -File .\studio-v2\start.ps1`
- 访问地址：`http://127.0.0.1:8810`
- 前台入口：`studio-v2/public/index.html`
- 服务入口：`studio-v2/server.mjs`
- API 入口：`studio-v2/src/local-api/api-routes.mjs`
- 单版/双版入口：`studio-v2/public/js/core.js` 的 `workflowModeOptions`，前台由 `studio-v2/public/js/render.js` 的 `renderWorkflowModeSwitcher()` 渲染。
- 新项目默认单版：`studio-v2/src/local-api/api-routes.mjs` 创建项目时写入 `workflowMode: "single_video"`。
- 视频生成入口：前台 `studio-v2/public/js/projects.js`，后端 `POST /api/projects/:projectId/videos/generate`。
- 生成状态入口：`GET /api/projects/:projectId/videos/status`。

## 开工前必读

所有执行端先读：

1. `AGENTS.md`
2. `CODEX_RUNBOOK.md`
3. `CURRENT_ASSIGNMENTS.md`
4. `docs/产品口径.md`
5. `docs/前后端接口契约.md`
6. `docs/验收清单.md`

## 当前工作顺序

1. 先保桌面网页端小白流程清楚。
2. 手机端暂不继续 polish，只保留基础不溢出、不重叠、不崩。
3. 最后补不影响流程的网页端动画、微交互和视觉 polish。

## 当前已完成验证

- Node 检查：`node --check studio-v2/public/js/render.js`、`core.js`、`main.js` 等通过。
- 自动化测试：`node --test studio-v2/tests/*.test.mjs`，63/63 通过。
- 浏览器验证：1440x900、1366x768、1280x800 桌面宽度无文档级横向溢出，控制台无 error/warn。
- 移动基础验证：390x844 无文档级横向溢出，控制台无 error/warn。
- Step 04 单版模式只显示 1 张故事版；Step 05 显示生成视频、视频预览和下载入口。
- 截图留档：`docs/codex-review-workbench/20260614-ui-1366x768.png`、`20260614-ui-1280x800.png`、`20260614-ui-390x844.png`。
- 1440x900 新项目 Step 01 复验：上传入口、单版/双版切换、右侧 AI 素材检测器均可见；左侧生成服务状态显示“本地演示可用”，不显示配置数量或技术词。
- P0 本地 API 烟测：`docs/codex-review-workbench/p0-flow-smoke.mjs` 自带 1x1 PNG，不依赖 `studio-v2/data/`；可从新建项目跑到 Step 04 故事版失败态；浏览器打开烟测项目时失败提示不暴露 API Key、供应商、模型、prompt 或绘图通道词。

## 口径收口

- 普通前台使用 `单版 5-15 秒 / 双版 20 秒`，不再使用 `单段/双段` 作为主按钮文案。
- 技术数据模式仍为 `single_video / legacy_multi_segment`，只出现在代码、schema 或接口契约中。

## 待用户确认

- README 和产品文档是否要从“开发者说明”改成“小白使用说明 + 管理员配置说明”双层结构。
