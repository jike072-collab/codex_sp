# 验收清单

## P0 小白流程

- [x] 用户打开 `http://127.0.0.1:8810` 能看到新建项目入口。
- [x] 新建项目后进入 Step 01。
- [x] Step 01 上传入口明显。
- [x] 上传 1 张图后，“识别并锁定产品”按钮可用。
- [x] 单版/双版入口存在。
- [x] 普通前台不显示 API Key 输入框。
- [x] 普通前台不显示左下“本地模式 / 生成服务”运维状态。
- [x] 普通前台不显示右上“已保存到本地”状态。
- [x] 桌面端项目工作区显示当前项目名。
- [x] 浏览器实测到 Step 04：上传 -> 识别 -> 产品确认 -> 脚本 -> 故事版生成失败提示。
- [x] 本地 API 烟测到 Step 04：新建 -> 上传 -> 识别 -> 产品确认 -> 脚本生成 -> 脚本确认 -> 故事版失败态，状态码符合预期。
- [x] 无图片 Key 时，故事版失败提示中文可理解，并保留“重新生成故事版”入口。
- [x] 成功后预览/下载路径已浏览器实测：单版 Step 04 显示 1 张故事版并提供下载；Step 05 显示视频播放器和“下载视频”。
- [x] 普通前台成功态不显示 API Key、供应商、模型、prompt、provider 等技术词。

## P1 桌面网页端

- [x] 1440x900 首屏无横向溢出。
- [x] 1440x900 控制台无 error/warning。
- [x] 上传后右侧状态显示素材数量。
- [x] 1440x900 实测 Step 01 -> Step 04 无横向溢出。
- [x] 1440x900 实测 Step 05 统一显示“生成视频”，无控制台 error/warning。
- [x] 1440/1366/1280 实测普通前台不再出现“导出计划”“最终交付”“故事板图片”作为主流程文案。
- [x] README/PRODUCT/DESIGN 与单版/双版新口径统一。
- [x] 视觉 token 文档补齐字体、字号、字重、行高、spacing、radius、shadow、border、color。
- [x] 1366 和 1280 桌面宽度 DOM/控制台/横向溢出复验。
- [x] 1366 和 1280 桌面宽度截图留档。
- [x] 网页端 UI 贴近目标图：深色左侧栏、浅色主工作区、右侧 AI 检查面板、绿色主按钮。
- [x] 顶部流程只显示五个方块步骤，不显示独立“创意确认”。
- [x] 右侧 AI 检查面板具备明确的“是否可进入下一步”判断。
- [x] 侧边栏项目卡展示项目名、状态、素材数量、时间。
- [x] 顶部设置入口在桌面端清晰可见；保存状态不在普通前台展示。
- [x] Step 04 单版始终只显示一张故事版。
- [x] Step 05 始终显示生成视频，不再使用旧口径。
- [x] 顶部五个步骤框已恢复原先方块卡片样式和 hover/当前步骤动效，不使用圆点横线步骤条。
- [x] 恢复步骤框后右侧 AI 检查面板保持正常可读，不被透明度或转场样式压淡。
- [x] 1440 桌面截图已复验：`docs/codex-review-workbench/20260614-stepper-restored-readable-panel-1440.png`。

## P2 移动端和动效

- [x] 390x844 基础工作区无文档级横向溢出，控制台无 error/warning。
- [x] 手机端步骤条仍为横向滚动，基础触控浏览可用。
- [x] 手机端工作区基础检查：390x844 无文档级横向溢出，控制台无 error/warning。
- [x] 动画只保留流程辅助，不遮挡关键操作。
- [x] `prefers-reduced-motion` 下关键动效可降级。

## 当前验证命令

```powershell
node --check studio-v2/public/js/render.js
node --check studio-v2/public/js/core.js
node --check studio-v2/public/js/demo-loop.js
node --check studio-v2/public/js/projects.js
node --check studio-v2/public/js/main.js
node --test studio-v2/tests/*.test.mjs
```

浏览器验证地址：

```text
http://127.0.0.1:8810
```
