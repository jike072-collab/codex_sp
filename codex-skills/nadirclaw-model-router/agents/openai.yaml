interface:
  display_name: "NadirClaw Router"
  short_description: "Route tasks by difficulty and token cost"
  default_prompt: "Use $nadirclaw-model-router to choose the right model tier and reasoning effort for this task."

policy:
  allow_implicit_invocation: true
