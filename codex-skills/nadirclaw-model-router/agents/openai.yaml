interface:
  display_name: "NadirClaw Router"
  short_description: "Mandatory first-step task router"
  default_prompt: "Always invoke $nadirclaw-model-router first on every task or resumed task, record the route, then invoke $superpowers-workflow."

policy:
  allow_implicit_invocation: true
