interface:
  display_name: "Superpowers Workflow"
  short_description: "Mandatory second-step repo workflow"
  default_prompt: "Always invoke $superpowers-workflow second on every task or resumed task, immediately after $nadirclaw-model-router, then inspect, implement, verify, and hand off."

policy:
  allow_implicit_invocation: true
